# --- Regenerable / heavy data (rebuilt by the pipeline) ---
# Raw harvest (~59 MB) and features (~73 MB): re-derivable. Not versioned.
pipeline/pipeline/cardlist_full.json
pipeline/pipeline/features_by_card.csv

# --- Cache and transient artifacts ---
__pycache__/
*.pyc
pipeline/_tr/
pipeline/_archive/
to_translate.json

# --- App build: ship only the compressed DB (the app fetches ws.sqlite.gz) ---
docs/ws.sqlite

# --- OS junk ---
Thumbs.db
.DS_Store
desktop.ini

# translation workflow temp
pipeline/_tr_batches/
pipeline/_tr_manifest.json
