# Runtime dependencies for ws-card-db.
# The pipeline core uses only the Python standard library; this covers the
# Excel output.
# Install with:  pip install -r requirements.txt
openpyxl>=3.1.5   # Excel output (pipeline/build_*.py)
